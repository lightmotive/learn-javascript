import { expect } from "chai";
import "mocha";

//Temporary demo test
describe("This", () => {
  describe("should", () => {
    it("always pass", () => {
      expect(true).to.equal(true);
    });
  });
});
